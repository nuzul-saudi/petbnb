Pill badge for host tiers, the honest "جديد" marker, status, and counts.

```jsx
<Badge label="ذهبي" tone="gold" />
<Badge label="جديد" tone="new" />
<Badge label="نشط" color="var(--moss-light)" />
<Badge label="٢" icon="🐈" tone="neutral" />
```

Tones: `gold` / `silver` / `bronze` (host tiers), `new`, `accent`, `danger`, `neutral`. Pass `color` to override with a status colour. Keep labels short — micro type, letter-spaced.
